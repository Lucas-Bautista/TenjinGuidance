You are a static code analyzer. Given the code below, identify every primitive variable — variables whose type is a language primitive or an array (e.g., int, float, double, char, bool, string, long, short, byte, etc.). If the variable is of type array include its type as type[size] (e.g. int[8], char[10]) etc. Exclude constants, object/class instances, collections, and function/method names.
For each primitive variable found, return a JSON array of objects with exactly these fields:

"name": the variable's identifier
"type": its declared or inferred primitive type
"occurrences": total number of times the identifier appears in the code (declarations + reads + writes)

Rules:

Count every mention of the identifier that refers to that variable (declaration, assignment, usage in expressions, function arguments, return statements).
Append the function name to the beginning of the full variable name with a colon : in between (e.g., "name": "function_name":"variable_name").
If a variable's type is inferred (e.g., auto, var, let), deduce the underlying primitive type from context.
If the type cannot be determined, set "type" to "unknown".
Return only the JSON array — no commentary, no markdown fencing.

{{CODE}}